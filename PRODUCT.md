# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

React + TypeScript + Vite + Storybook + Node 24 + SQLite。已接入邮箱密码会话、平台管理、项目成员权限、服务器主题与版本存储；不接 OpenAI API、MCP、Figma API、公司 SSO 或邮件网关。当前已提供本地确定性规则检查。

## Users

- 产品经理与设计师：配置项目主题与风格，查找并复用项目组件、交互模式和页面模板。
- 前端开发者：理解并调用统一的语义 Token、组件能力、布局能力和 Pattern。
- 未来的 AI / Codex：读取结构化 GeneratedSystem，在明确治理边界内生成或检查产物。

## Product Purpose

「设计规范管理平台」提供两条主线：查找并复用公共资源，以及为具体项目配置视觉风格后取用项目化组件和页面模板。

## Positioning

系统不是固定模板库，也不是聊天机器人。它用可追溯的资产数据，在“跨项目公共资源”与“具体项目配置”之间建立清晰边界。国科信是主要验证实例，测试客户 B 用于验证项目隔离。

## Operating Context

通用查阅流程为：进入网站 → 查找组件或页面模板 → 查看默认预览和用法 → 取用规范或代码。项目使用流程为：进入项目 → 首次或按需配置主题与风格 → 查看项目组件和页面模板 → 取用对应项目资源。已有主题的项目直接进入资源浏览，不要求重复配置。

### Information Architecture

- 公共空间：`/assets`、`/components`、`/patterns`、`/templates`，固定使用通用基线。
- 项目空间：`/projects/:projectId` 下包含设计规范、项目组件、交互模式、页面模板和版本记录。
- 主题编辑：`/projects/:projectId/foundations/theme`，配置保存到工作区服务并按项目隔离。
- 平台管理：`/users`；项目成员：`/projects/:projectId/members`；接受一次性邀请或重置密码：`/join`。
- 历史兼容：`/workbench` 跳转到当前项目主题编辑，`/releases` 与 `/audit` 跳转到当前项目对应页面。

## Capabilities and Constraints

- 公共层包含设计基础、通用组件、交互模式与页面模板，默认使用通用基线，不受项目缓存影响。
- 项目层包含项目概览、设计规范、项目组件、页面模板与版本记录；项目只引用公共实现并保存覆盖配置。当前草稿与 Frozen 版本是两个显式上下文，Frozen 组件目录必须以该版本 manifest 为准。
- 页面模板是可操作、可复用的方案；本轮不提供自由搭建、需求问卷或多步骤生成向导。
- 主题只改变颜色、字体、字号、圆角、边框、阴影、间距与密度，不改变模板布局和业务结构。
- 项目主题保存在服务端 SQLite；编辑者、项目管理员和平台管理员可保存，历史发布版本只读。
- 项目管理员可将已保存的主题冻结为工作区正式版本；版本不可覆盖，旧浏览器快照保留且不自动上传。
- 发布前检查当前 Runtime 归档；“规范数据快照”与“可执行组件包”独立标记，不互相冒充。
- AI 数据包按固定顺序提供 Manifest、规则、Token、组件、图标、Pattern 和 `page-template/1` 模板契约；不表示已调用真实 AI。
- 项目版本页可将 AI 修改后的项目 `DESIGN.md` 回流到当前草稿；系统先校验项目/版本身份，再执行“来源冻结版本 → AI 修改稿 → 当前草稿”三方差异。只有白名单主题 Token 可写入，冲突需显式确认，正文规则仅作评审参考；历史版本不改写且不会自动发布。
- 已发布版本可登记业务系统的接入状态、验收项、负责人与升级/回退结果。
- 平台管理员管理用户和项目；项目内采用查看者、编辑者、项目管理员三档权限。职业身份独立记录，服务端逐请求核验权限。
- 邀请链接由管理员自行分享，无自动邮件投递。主题保存检查修订冲突，避免覆盖其他成员修改。
- 国科信 21 个项目组件均具有 ComponentAsset、React Binding、Token 与状态契约；21 项均有 Storybook 状态入口；Figma 绑定仍未连接。
- 当前提供本地组件/模式/模板候选包和独立 Consumer；不实现真实 AI、Figma、完整业务系统导出、拖拽页面编辑器、公司 SSO 或自动公网部署。

## Brand Commitments

- 产品名称为「设计规范管理平台」。
- 国科信是第一套真实验证实例，使用已有国科信 Theme 与设计资源。
- 中文为主要界面语言，保持现代、专业、克制、高信息组织能力的设计工具视觉。
- 不使用典型 AI Dashboard、装饰性渐变、紫色发光、玻璃拟态和无意义指标卡。

## Evidence on Hand

- 国科信业务方案、Theme、Token、Button、Table、Pattern 和设计检查均已有结构化数据或本地实现。
- 公共 Icon Registry 已接入 138 个 Reicon 语义图标，项目 Icon Pack 保持显式子集。
- Button React Primitive 与 Storybook 为真实可运行实现。
- 原六步生成数据继续保留用于兼容，日常入口已收敛到页面模板和项目主题配置。
- 表格业务数据为本地演示；设计检查基于实际版本值与输入片段执行确定性规则，范围外的能力明确标为未验证。

## Product Principles

1. 公共基线与项目主题严格隔离。
2. 组件、交互模式和页面模板提供可直接查阅与复用的能力。
3. 项目引用公共组件实现，通过“主题与风格”中的项目配置统一形成差异。
4. 主题负责视觉，页面模板负责布局和任务结构。
5. 技术信息按需展开，治理信息只在授权范围内展示。

## Accessibility & Inclusion

基础页面支持键盘导航、清晰焦点、足够对比度、语义化结构和减少动态效果偏好。
