# 账号与项目权限验收

日期：2026-09-19。

实现范围：Node 24 / SQLite 会话与存储、首位管理员初始化、平台用户与项目管理、项目成员和邀请、角色校验、共享主题修订冲突、项目版本发布与读取。公司 SSO、邮件投递和公网部署未实施。

验证结果：

- 前端与现有逻辑：43 个 Vitest 文件、695 项测试通过。原冻结版本竞态测试新增真实会话接口的测试响应，未移除原有断言。
- 服务端：12 个独立 HTTP 子场景通过（Node runner 总计 13 tests）；涉及账号会话、跨站写入、项目隔离、角色越权、邀请、冲突、发布、最后管理员、停用、重置及重启持久化。
- 前端与服务端 TypeScript 检查通过。
- npm run build 通过；仍有既有的大 chunk 体积提示。
- lint 为 0 errors / 7 Fast Refresh warnings。
- 新增管理与登录页面的 Impeccable 静态检测结果为 []。

浏览器使用独立临时 SQLite，端口 5180，未写入实际工作区：

1. 使用验收管理员账号实际登录，账号菜单显示平台管理。
2. 用户页展示真实账号和停用限制，项目页成功创建“权限验收项目”，并出现成员入口。
3. 成员页展示创建者的项目管理员角色，生成查看者邀请后显示链接与待接受记录。
4. 接受邀请由 HTTP 测试执行；随后在浏览器使用产品经理/查看者账号登录，项目列表仅展示受邀项目。
5. 查看者直接打开成员管理与主题编辑地址，分别显示需要管理权限和只读权限，无编辑或成员操作入口。
6. 成员页窄屏检查：约 397px CSS 视口下，说明、邀请字段及按钮为单列，表格在内部滚动；导航采用原有窄屏滚动布局。补充移动端账号按钮的可访问名称。
7. 查看者页面浏览器错误日志为空。退出后返回登录。
8. 临时服务、数据库和会话已清理，viewport 已恢复。

实际服务保留于 127.0.0.1:5174，经现有 Vite 5173 代理。实际网站已确认呈现“建立你的工作区”，auth/status 返回 needsSetup=true。管理员邮箱和密码由使用者自行设置，不含验收账号。旧浏览器存储及原冻结资产未删除或自动上传。
