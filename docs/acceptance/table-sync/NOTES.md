# 表格样式同步

保留之前组件页的灰色容器、白色内容区、容器圆角、38px 表头及 16px 单元格左右间距，将这些已存在的样式迁入共享 Table.css。统一实现不等于还原未修饰的基础表格。

组件页、管理示例、列表模板与新交付包引用同一 Runtime；组件页不再覆盖内部表格皮肤，管理示例不再手写原生表格、复选框与分页。选中态仍使用主题选中色，勾选居中、固定列和分页右对齐保留。

历史冻结包不重写；运行时源代码更新需要生成匹配的新版本包，已安装的旧包不会自动被替换。

## 圆角变量

- 新增 `radius-table`，默认 6px；项目主题「尺寸外观」提供 0–24px 的独立配置。
- Runtime 外层使用 `--bds-table-radius`，内容区使用 `--bds-table-content-radius = max(0px, radius-table - 2px)`。普通控件圆角不受影响。
- 变量注册、组件绑定、DOM 实测、项目设置保存/校验、旧主题兼容、新快照、tokens.json/CSS 与生成的 DESIGN.md 均接入。
- 真实项目配置页实测：滑块改为 12，预览外层 12px、内容区 10px；随后撤销，未保存用户主题。
- 独立预览中组件/管理/模板三处实测一致：默认外层 6px、白色内容区 4px；12px 对应 10px；0px 对应 0px。
- 实际 `/components/table` 已确认共享样式与圆角实测项生效。勾选图形两轴中心误差 < 0.01px，分页与管理表格外框右侧差 < 0.01px。

## 检查

- 新圆角与迁移相关定向回归 31 项通过，TypeScript 与最终构建通过。
- 完整前端运行 747 项通过，1 个旧版本切换测试在并行构建时超时；同一用例单独复查通过，未调整超时时间或断言。服务端权限/交付 31 项通过。
- 三份隔离项目版本的设计 MD 均通过真实组件包的独立安装、TypeScript 编译和错误调用反例检查。
- 旧发布保持冻结；源码更新不会改写用户已经安装的历史包，需要采用新版本包。
