> 本记录为上一轮验收。最终复核已发现遗漏，当前结论以 [最终复核与修复](./FINAL-RECHECK.md) 为准，不能继续用本记录中的全部完成声明放行。

# 本地稳定候选验收

日期：2026-09-19。结论：**R01–R20 在声明的本地范围内已完成并通过验收**。候选为 `0.0.0-local.20260919`，没有执行正式云端发布。

## 自动检查

|检查|结果|
|---|---|
|主工程回归|38 个文件，640 项通过|
|主工程构建与类型|通过；Token、Button Binding、27 条历史资产检查及文档生成通过|
|Lint|0 错误，8 条原有 Fast Refresh 警告|
|核心 Storybook|20 个组件、103 个状态/变体 Story 构建并浏览器渲染成功；原 Button 契约 Story 保留|
|本地候选|11 项资产；10 个载荷文件的实际字节数与内容校验通过，manifest 为清单本身|
|Consumer|从 tarball 安装到独立依赖目录，TypeScript 与 Vite 构建通过|
|运行时一致性|候选 metadata、包内代码/快照和 Consumer 安装内容标识一致|

测试、构建、Lint、Storybook、候选打包及 Consumer 日志保存在本目录；机器可读结果见 [verification.json](./verification.json)。103 项 Story 渲染不被当成 103 项完整业务验收，行为和组合路径由下述操作及专项测试补充。

## 浏览器与组合证据

- 主站项目模板：筛选“政策”→查看详情→修改名称→模拟一次提交失败→保留输入→重试成功→返回列表；筛选仍保留，原行已更新。
- 12 个模式均存在正式 DS 可执行业务组合，使用相同项目主色；Loading 和 Error 模式的完成/重试操作可恢复列表。
- 核心控件采用前两轮状态矩阵及本轮 [API—状态—示例—测试映射](./CORE-COMPONENTS.md)。已复验的组合包括错误+hover、禁用+选中、浮层主题/键盘焦点、清空保护、关闭重开、上传大小失败恢复。
- 新 local-2 快照可经过现有 Provider 和 PreviewScope 渲染，按钮背景/边框均为 `#165DFF`。历史 v1.5.5 原色继续保持，不回写旧包。
- 版本页面比较 v1.5.4 → v1.5.5，读取出 5 项实际资产差异，没有不可读资产；不再只比较索引标识。
- 无效 `?version=99.0.0` 保持原 URL 并显示“版本不可用”，不会换成另一个版本。
- 备份 UI 在独立来源 `127.0.0.1:5174` 测试：无效 JSON 被拒绝，有效文件显示范围后恢复，页面重载后项目主色为备份中的 `#6236FF`。未覆盖用户日常 `5173` 来源的数据。服务级测试覆盖同号冲突、关联失败和写入回滚。
- 390px 实际 CSS 视口：固定组件侧栏隐藏，目录可展开切换且保留版本；正文宽约 345px。模板表格在 303px 容器内滚动 600px 内容，根节点无横向溢出。
- 独立 `4178` Consumer：运行时与快照匹配，按钮背景和边框为 `#165DFF`；筛选“专家”后编辑为“专家资源最终验收”，保存后在原列表出现。
- Consumer 曾通过版本检查发现旧 Vite 预构建缓存；现已增加不匹配时停止展示，并在开发命令使用 `--force`。最终已重新安装和冷启动复验。
- 本地规则页对真实输入 `<DSUnknown />` 和未知变量分别报告第 1、2 行；占位对比度 4.56:1，品牌白字 5.19:1。

## 交付与使用

- [本地包和独立取用说明](../../LOCAL-STABLE.md)
- [项目任务状态](../../PROJECT-PLAN.md)
- 候选归档与 SHA-256：`artifacts/local-stable/delivery.json`
- 包含 TypeScript 声明、冻结变量、源码标识和第三方许可说明。

历史 Token、图标记录和版本文件未改写。新浏览器快照捕获数据，执行代码由匹配 build ID 的候选包提供；旧版本缺少执行代码归档时，明确保留兼容边界。

## 外部扩展状态

Figma 变量/组件交付 JSON、协作 If-Match/冲突接口及 AI 评审代理适配器已准备，本地确定性规则已实现。**实际 Figma 写入、协作服务器部署、真实 AI 调用尚未完成**：用户尚未提供目标 Figma 文件/团队、部署环境和 AI 服务配置。详见 [外部扩展说明](../../EXTERNAL-INTEGRATIONS.md)。不把接口或演示称为已上线服务。

本结论不涵盖生产鉴权、多人并发服务、完整 WCAG 认证或 88 个公共组件的全部可能组合。已知非阻断项为现有 Fast Refresh 警告及字体/兼容 Runtime 导致的较大包体。
